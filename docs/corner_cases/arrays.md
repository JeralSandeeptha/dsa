# Hints

non-increaing --> can have duplicate with sorted increaing array

non-decreaing --> can have duplicate with sorted decreaing array

in-place      --> we dont want to create a new one instead of that we can change the existing one / we can create new arrays fir fix solutions
